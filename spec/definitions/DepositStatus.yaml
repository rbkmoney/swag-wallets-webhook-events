type: object
properties:
  status:
    description: |
      Статус пополнения.

      | Значение    | Пояснение                                  |
      | ----------- | ------------------------------------------ |
      | `Pending`   | Пополнение в процессе выполнения           |
      | `Succeeded` | Пополнение средств произведено успешно     |
      | `Failed`    | Пополнение средств завершилось неудачей    |

    type: string
    enum:
      - Pending
      - Succeeded
      - Failed

  failure:
    x-rebillyMerge:
      - description: |
          > Если `status` == `Failed`

          Пояснение причины неудачи
        readOnly: true
      - $ref: '#/definitions/DepositFailure'
