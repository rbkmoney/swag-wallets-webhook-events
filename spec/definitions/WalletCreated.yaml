description: Событие о pаведении нового кошелька
allOf:
  - $ref: '#/definitions/Event'
  - type: object
    required:
      - wallet
    properties:
      wallet:
        $ref: '#/definitions/Wallet'