description: Данные источника
allOf:
  - type: object
    required:
      - name
    properties:
      id:
        x-rebillyMerge:
          - $ref: '#/definitions/SourceID'
      name:
        x-rebillyMerge:
          - $ref: '#/definitions/SourceName'
      createdAt:
        description: Дата и время создания источника
        type: string
        format: date-time

      resourceInternalDetails:
        x-rebillyMerge:
          - $ref: '#/definitions/SourceName'

