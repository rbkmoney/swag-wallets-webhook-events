description: Событие о изменении счета
allOf:
  - $ref: '#/definitions/Event'
  - type: object
    required:
      - account
      - sourceID
    properties:
      account:
        $ref: '#/definitions/Account'
      sourceID:
        x-rebillyMerge:
          - $ref: '#/definitions/SourceID'