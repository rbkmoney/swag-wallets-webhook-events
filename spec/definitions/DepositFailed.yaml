description: Собтыие неуспешного пополнения
allOf:
  - $ref: '#/definitions/Event'
  - type: object
    required:
      - depositID
    properties:
      depositID:
        x-rebillyMerge:
          - $ref: '#/definitions/DepositID'