description: Событие о деавторизации источника
allOf:
  - $ref: '#/definitions/Event'
  - type: object
    required:
      - sourceID
    properties:
      sourceID:
        x-rebillyMerge:
          - $ref: '#/definitions/SourceID'
